# しゅがけブリング — NOTES

SPEC.md / INSTRUCTIONS.md は旧名「クローリング」で書かれている。**ゲーム名はブリングが正**。
表示名「しゅがけブリング」、セーブキーは `sc_bring_v1`（SPEC の `sc_crawling_v1` から変更）。

## 2026-09-24 Proto1〜4 一括（index.html 1枚）
- 変えた数値: 仕様値のままだと単独スタートで B1〜2 で全滅するため仮調整（CONFIG に `/*SPEC x*/` で元値を併記）。
  `player.regenEvery 10→5` / `feral.hpBase 40→20, atkBase 6→3, atkPerFloor 0.8→0.5` /
  `dog.atkBase 3→2, atkPerFloor 0.5→0.3, pack [2,3]→[1,2]` / `poacher.atkBase 4→3, atkPerFloor 0.7→0.4` /
  `guard.atkBase 5→4, atkPerFloor 1→0.5` / `spawn.enemies [4,7]→[3,5]`
- 気づいたこと: レベルがなく敵だけ階層で強くなるので、20階は現状ほぼ届かない（ボットの中央値 B3）。/
  拠点に誰もいなくなる（全員 feral/dead）と詰む（今は「記録を消す」しかない）。/ 390px だとタイルが約9px で小さい。
- 次にやること: 深層バランス（W による ATK 上昇だけで足りるか）／詰み時の救済の是非／マップの拡大表示。

## 2026-09-24 べにの指摘で操作と生成を変更
- 攻撃はコマンド。方向キーで敵/壁の方へ押すと「向く」だけ（ターン消費なし）。「攻撃」（パッド中央・`a`/Space/Enter）で向いている方を殴る。空振りもターン消費。
- 敵は1歩に1歩: `dog.speed 2→1`, `guard.speed 0.5→1`（SPEC の速度差を廃止）。一度プレイヤーを見つけた敵は見失わずに追う（檻番だけは SPEC どおり部屋から出ない）。
- 部屋は常に長方形: 部屋同士を3マス離し、通路は部屋の外周の出入口1マスでだけ接続。斜め移動・斜め攻撃は角をすり抜けられない（トルネコ式）。
- `player.regenEvery 5→2`（継続戦闘できるように）。ボットの到達階 中央値 B3→B5、最高 B17。死因は檻番が最多に。
- 表示はキャラ中心の 13×11 マス（`view.cols/rows`）。

## 2026-09-24 視界と追跡
- 視界は敵味方共通（`canSee`）: 部屋の中と出入口 → その部屋全体、通路 → 隣8マス。
- 敵の状態: 発見（見えている間は追う）→ 見失う（`ai.memory` ターンは気配で追い、その後は最後に見た場所へ）→ うろつき（どこかの部屋を目的地に歩く）。
  `ai.giveUp` ターン進めなければ諦める。檻番は SPEC どおり部屋から出ない。
- 真後ろについてくる敵は振り切れない（同じ速さ）。数マス離れていれば通路で撒けることがある（5歩差で約3割）。

## 2026-09-24 描画をトルネコ式に
- 「四角形以外の部屋」は生成ではなく描画の問題だった（壁を床より明るく塗っていたため、部屋の外周と通路の壁がつながってT字・L字に見えた）。
  壁は描かず、床と通路だけ描くように変更。
- メイン画面（キャラ中心）は画面内の地形を常に全部描く。視界の外は `view.dimAlpha` で暗く。敵とアイテムは視界の中だけ。
- フロアマップ: HUD の「地図」/ `m` キーで重ね表示を出し入れ。行ったことのある場所・階段（見つけていれば）・今見えている敵とアイテム・自分。
  出し入れの状態はブラウザに保存（`sc_bring_v1_ui`）。

## 仕様の解釈（SPEC に書いてなかったところ）
- 野性化アマルに「弱っていない」状態で糖を与えると、糖を食べて HP が回復する（仲間にはならない）。
- 特上ケーキを弱った野性化アマルに与えると、仲間化＋ wildMin を 0 に戻す。
- 「置く」を追加（SPEC 5.2「糖を落とすと寄っていく」を実現するため）。
- 階段は踏んだら即降りる。
- ラン途中でブラウザを閉じた場合、同行者は拠点（roster）に戻す。
- 今ランで失った仲間は、同じランの中では敵として出さない（次ラン以降に出す）。
- アイテム操作は「アイテム」ボタンを開くメニューではなく、所持品を常時表示してタップで選ぶ形にした。
  キーボード: 矢印/テンキーで移動・向き、`a`/Space/Enter で攻撃、`m` 地図、`.`/5 で待機、`s` 交代、`i` 糖の選択、`e` 食べる、`g` 与える、`d` 置く。
