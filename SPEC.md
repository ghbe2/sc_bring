# クローリング (sc_crawling) — SPEC.md
Sugar Cage ゲームシリーズ / 1枚HTMLローグライク / MVP版
最終更新: 2026-09-23

## 0. コンセプト（1行）
檻の中で獣になった仲間を、糖で人に戻して連れ帰る。使うほど強く、使うほど飲まれる。

## 1. スコープ（MVP・これ以上足さない）
1. ランダムマップ・ターン制・敵3種＋野性化アマル・階段降下・20階到達でクリア
2. 野性ゲージ: 攻撃で上昇。上がるほど攻撃力UP。100で四足化＝離脱
3. 糖（お菓子）: 食べると野性減・HP回復。野性が下がる＝弱くなる
4. 交代: 1ターン消費。控えは野性が徐々に下がる
5. 野性化アマル: HPを削って糖を使うと仲間化。殺すと永久ロス＋強い報酬
6. localStorage に名簿状態を保存。失った仲間は次ラン以降、敵として出る

**やらないこと**: キャラ固有能力／レベル／満腹度／店／アイテム識別／持ち込み／ボス／装備

## 2. キャラクター（8人・身体性能は全員同一）
差は野性の「上がりやすさ」「下がりやすさ」の2係数のみ。

| id | 名前 | 初期陣営 | wildUp | wildDown |
|---|---|---|---|---|
| gessha | 月謝 | 選択可 | 1.0 | 1.0 |
| kona | こな | 選択可 | 0.8 | 0.8 |
| riu | リウ | 選択可 | 1.2 | 1.2 |
| aune | アウネ | 選択可 | 1.0 | 1.3 |
| ou | オウ（依鳥） | 初期野性化 | 1.3 | 1.0 |
| amime | アミメ | 初期野性化 | 1.2 | 0.8 |
| torotoro | トロトロ | 初期野性化 | 0.8 | 1.2 |
| maan | mAAn | 初期野性化 | 1.0 | 1.0 |

- 初回はゲッシャ／こな／リウ／アウネから1人選択。残り7人は `feral`
- オウの野性化形態は「本体オウ」。プレイアブルは「依鳥オウ」。表示名・アイコンを切り替える（MVPは文字表示）

### 2.1 名簿状態（永続）
```
status: 'party' | 'roster' | 'feral' | 'dead'
wildMin: 0 | 20 | 40 | 60      // 野性の下限。飲まれるたび +20、上限 60
```
- `party`: 今ランの同行（最大3人）
- `roster`: 拠点で待機。ラン開始時に3人まで編成
- `feral`: 檻の中で獣。ランダム出現の敵になる
- `dead`: 殺された。以後出ない（永久ロス）

## 3. ステータス
- レベルなし。強さの源は野性のみ
- 全キャラ共通: `HP_MAX 30`, `BASE_ATK 5`, `DEF 0`
- HP自然回復: 10ターンに1
- 野性 W: 0〜100（下限は wildMin）

```
ATK = BASE_ATK + floor(W / 10)          // W=90 で 14
```

### 3.1 野性の増減
- 自分の攻撃が命中: `W += 3 * wildUp`
- 歩行では上がらない（**能動的に使うと上がる**を守る）
- 控え中: 5ターンごとに `W -= 1 * wildDown`（wildMin まで）
- `W >= 80`: 高揚状態。ログに警告、ゲージ色変更
- `W >= 100`: **四足化**。即離脱 → `status = 'feral'`, `wildMin += 20`

### 3.2 HP 0
- 同行キャラの HP 0 = 檻に呑まれる → `status = 'feral'`, `wildMin += 20`
- 控えがいれば自動で交代（1ターン消費なし）。控え0で **ラン終了**
- ラン終了時、拠点の `roster` は無事。`party` は全員 `feral` になっているのでそのまま

## 4. 糖（アイテム）
拾う → 自分に使う（隣接する野性化アマルには「与える」）。消費アイテムはこの4種だけ。

| 名前 | W減 | HP回復 | 出現 | 備考 |
|---|---|---|---|---|
| 角砂糖 | -5 | +3 | 多 | |
| 飴 | -15 | +8 | 中 | |
| ケーキ | -40 | +20 | 少 | |
| 特上ケーキ | 全減（wildMinまで） | 全快 | ドロップのみ | 使った相手の `wildMin` を 0 に戻す。野性化アマルを**殺した時だけ**落ちる |

- W が wildMin 未満には下がらない（余剰は無効）
- 所持上限 8

## 5. 敵
### 5.1 一般敵（階が深いほど強い。`floor` は階数）
| 名前 | HP | ATK | 速度 | 特徴 |
|---|---|---|---|---|
| 野犬 | 8 + floor | 3 + floor*0.5 | 2倍速 | 群れで出る（2〜3） |
| 密猟者 | 14 + floor*1.5 | 4 + floor*0.7 | 通常 | 隣接時、攻撃の代わりに糖を1個盗む（30%） |
| 檻番 | 24 + floor*2 | 5 + floor*1 | 半速 | 硬い。追ってこない（部屋固定） |

### 5.2 野性化アマル
- `feral` のキャラから1人ランダム選出。1階に最大1体
- 出現率: 1〜3階で**必ず1体**出す（チュートリアル）。以降は各階 30%
- 能力値: `HP 40 + floor*2`, `ATK 6 + floor*0.8`, 通常速。糖を落とすと寄っていく（餌に釣られる）
- 表示: キャラ名＋「(野性)」

### 5.3 連れ戻す／殺す
- HP が **30%以下** で「弱っている」表示
- 弱っている状態で隣接し、糖を与える → 仲間化 `status = 'party'`（同行3人以上なら `roster` へ）。
  戻ったキャラの W は `wildMin` から開始
- HP 0 → `status = 'dead'`。**特上ケーキをドロップ**
- 殺す方が楽で報酬が強い。戻す方が手間で仲間が増える。この選択をログで煽らない（プレイヤーに委ねる）

## 6. マップ
- 20階。階段で降りる（上り不可）
- グリッド 40×24。部屋 4〜8個（各 3〜8 × 3〜6）、通路で全部屋を連結（生成法は INSTRUCTIONS 参照）
- 視界: 部屋内は部屋全体、通路は隣接1マスのみ。探索済みは薄表示
- 1階あたり: 敵 4〜7、糖 3〜5、階段1

## 7. ターンと操作
- 8方向移動。移動先に敵がいれば攻撃。命中率 90%（敵も同じ）
- ダメージ: `max(1, ATK - DEF + rand(-1,1))`
- 行動: 移動／攻撃／待機／アイテム使用／交代（1ターン）
- 操作: 画面下の8方向パッド＋中央「待つ」。上に「交代」「アイテム」。キーボードは矢印＋テンキー、`.`待機、`s`交代、`i`アイテム

## 8. UI（スマホ縦 390px 基準）
```
[上] マップ canvas（正方形タイル、画面幅に合わせる）
[中] ログ3行
[中] 同行3人の 名前 / HP / Wゲージ（アクティブを強調・W>=80 で赤）
[下] 8方向パッド、交代、アイテム
```
- キャラは文字1〜2字＋色で表示（画像は後日差し替え前提）
- 拠点画面: 名簿一覧（party/roster/feral/dead と wildMin）、3人編成、出発

## 9. セーブ
- `localStorage` キー `sc_crawling_v1`
- 保存内容: 名簿8人の `status`/`wildMin`、クリア回数、最深到達階
- 保存タイミング: ラン終了時・クリア時・仲間化時・四足化時・死亡時
- ラン途中の中断セーブは MVP では **やらない**（閉じたらラン終了扱い）

## 10. クリア条件
- 20階の階段に到達 → クリア。`party` は全員 `roster` へ、W は wildMin にリセット
- 初回クリア後も同じダンジョンを繰り返す（収集は副目標）

## 11. 用語（World_Setting 準拠）
- 野性化＝アマルが動物因子に飲まれ「二足歩行のプライド」を失う状態（人語不可）
- 糖＝抑制。社会がアマルを「檻」に留めるための甘さ。それで仲間を人に戻す
