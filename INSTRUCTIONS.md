# クローリング — INSTRUCTIONS.md（実装手順）
対象: Claude Code。SPEC.md が仕様の正典。ここは作り方と受け入れ条件。

## 0. 前提
- 成果物は `index.html` **1ファイル**。CSS/JS内包。外部ライブラリ・外部アセットなし
- フレームワークなし。Vanilla JS（ES2020）、`<canvas>` 描画
- スマホ縦（幅390px）を第一。PCでも崩れないこと
- 画像は使わない。キャラは文字＋色。差し替えやすいよう描画関数を1箇所に集約

## 1. CONFIG-first
ファイル先頭に **全数値** を `CONFIG` オブジェクトとして置く。SPEC の表の値はすべてここ。
ロジック内に数値リテラルを書かない（`0`, `1` の制御用を除く）。

```js
const CONFIG = {
  map: { w: 40, h: 24, rooms: [4, 8], roomW: [3, 8], roomH: [3, 6], floors: 20 },
  player: { hpMax: 30, baseAtk: 5, def: 0, regenEvery: 10, partyMax: 3, bagMax: 8 },
  wild: { perHit: 3, benchDecayEvery: 5, benchDecay: 1, highAt: 80, feralAt: 100, minStep: 20, minCap: 60 },
  hit: 0.9,
  candy: { sugar:{w:5,hp:3}, drop:{w:15,hp:8}, cake:{w:40,hp:20} },
  feral: { hpBase:40, hpPerFloor:2, atkBase:6, atkPerFloor:0.8, weakRatio:0.3, guaranteeFloors:3, spawnRate:0.3 },
  enemies: { /* SPEC 5.1 の3種 */ },
  spawn: { enemies:[4,7], candy:[3,5] },
  chars: [ /* SPEC 2 の8人: id, name, feralName, side, wildUp, wildDown, color, glyph */ ],
  saveKey: 'sc_crawling_v1',
};
```

## 2. 段階（Proto）
各 Proto が動いてから次へ。1つの Proto ごとにコミット。

### Proto1: 歩ける
- マップ生成（下記 3）／プレイヤー1人／8方向移動／階段で次階／20階で「クリア」表示
- 視界と探索済み表示
- 受け入れ: 20階まで詰まらずに降りられる。全部屋が連結（生成後に到達可能性チェックを入れ、失敗なら再生成）

### Proto2: 殴れる
- 一般敵3種・出現数・速度差（2倍速＝1ターン2行動、半速＝2ターンに1行動）
- 攻撃・被弾・HP0でラン終了（この時点では単独）
- 野性 W の上昇と ATK 連動。W>=80 警告、W>=100 で離脱（この時点ではラン終了）
- 受け入れ: 殴り続けると W=100 で必ず離脱する。W とダメージがログで確認できる

### Proto3: 檻のループ
- 糖4種・拾う・使う・W減・HP回復・wildMin 下限
- 野性化アマル出現（1〜3階保証）・弱り判定・糖を与えて仲間化・殺して特上ケーキ
- 同行3人・交代（1ターン）・控えの W 減衰・HP0/W100 で自動交代
- 受け入れ: 1ランで「仲間化」「殺害→特上ケーキ」「四足化→離脱」の3イベントを全部再現できる

### Proto4: 続く
- localStorage 保存／読込・拠点画面（名簿・編成・出発）
- 失った仲間が次ランで敵として出る。`dead` は出ない
- クリア時の処理（roster 戻し・W リセット）
- 受け入れ: ブラウザを閉じて開き直しても名簿が残る。前ランで四足化したキャラが敵として出現する

## 3. マップ生成（簡潔版）
1. `rooms` 個の矩形をランダム配置。重なり・接触は再抽選（上限回数超えたら少ない部屋数で確定）
2. 部屋を x 座標でソートし、隣同士を L字通路で接続。確実に連結する
3. 追加で 1〜2 本ランダム接続（ループを作る）
4. 階段は開始部屋以外の部屋に置く
5. 生成後、開始位置から BFS で階段到達を確認。不達なら再生成

## 4. ターン処理順
```
プレイヤー行動 → 敵行動（速度に応じて0/1/2回） → 経過処理（HP regen / 控え W 減衰 / 状態判定）
```
- 状態判定の順: HP0 → W100 → 自動交代。同ターンで複数発生しても1体ずつ処理

## 5. 敵AI（最小）
- 視認（同じ部屋 or 隣接）で追跡。BFS 最短で1歩。視認外はランダム歩行
- 野犬: 上記＋2倍速。密猟者: 隣接時 30% で盗み（糖1個消滅、盗んだら1ターン逃走）
- 檻番: 自室から出ない。侵入者に隣接で攻撃
- 野性化アマル: 床の糖が視界内にあればそこへ向かう（プレイヤーより優先）

## 6. 描画
- タイル: 床／壁／通路／階段／糖／敵／プレイヤー。探索済み未視認は 40% 明度
- キャラ glyph: 名前1文字（月／こ／リ／ア／オ／ミ／ト／m）。野性化は同 glyph に赤枠
- ログは直近3行。仲間化・四足化・死亡は色付き

## 7. 禁止
- SPEC にない機能を足さない（能力・レベル・満腹度・店・装備・ボス）
- CONFIG 以外に数値を散らさない
- 画像・音・外部フォント・CDN
- ラン途中セーブ

## 8. 引き継ぎ
- 各 Proto 完了時、`NOTES.md` に「変えた数値・気づいたバグ・次にやること」を3行で残す
- バランス調整の相談は「どの CONFIG 値を何から何へ」の形で出す
